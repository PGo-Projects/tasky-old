# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Exclude node_modules, Js packages can be reinstalled with `npm install`
node_modules

# Exclude produced static assets
web/public/

# Exclude tern config file(s) used when editing JS
.tern-project
