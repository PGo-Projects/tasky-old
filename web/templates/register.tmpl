{% define "title" %}Register{% end %}

{% define "head-addon" %}
{% end %}

{% define "content" %}

	{% $errorMessage := "" %}

    {% with .errors %}
        {% with (index . "") %}
			{% $errorMessage = (index . 0) %}
        {% end %}
    {% end %}

	<app data-csrf-token="{%.csrf_token%}" data-error-message="{%$errorMessage%}"></app>

    {% asset "register.js" %}

{% end %}
